apiVersion: v1
kind: Service
metadata:
  name: test-repo-service
spec:
  selector:
    app: test-repo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer # Use ClusterIP if you don't need external access